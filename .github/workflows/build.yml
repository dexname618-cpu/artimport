name: Build Mod

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    uses: geode-sdk/build-geode-mod/.github/workflows/build.yml@main
    with:
      combine: true
      platforms: android64
    secrets: inherit
